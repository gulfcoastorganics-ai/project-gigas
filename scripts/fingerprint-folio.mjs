import fs from 'node:fs'; import path from 'node:path'; import crypto from 'node:crypto'
const root = path.resolve(import.meta.dirname, '..'); const arg = process.argv.find((item) => item.startsWith('--folio=')); const folio = arg?.split('=')[1]; if (folio !== '002r') { console.error('ERROR: only --folio=002r is configured in Sprint 6.'); process.exit(1) }
const files = ['src/data/layouts/folio-002r.json','src/data/regions/folio-002r.json','src/data/transcriptions/folio-002r-diplomatic.json','src/data/transcriptions/folio-002r-expanded.json','src/data/translations/folio-002r-literal-en.json','src/data/translations/folio-002r-readable-en.json','src/data/alignments/folio-002r.json','src/data/uncertain-readings/folio-002r.json','src/data/reviews/assignments-002r.json','src/data/reviews/findings-002r.json','src/data/change-history/folio-002r.json']; const canonical = Object.fromEntries(files.map((file) => [file, JSON.parse(fs.readFileSync(path.join(root, file), 'utf8'))])); const normalize = (value) => Array.isArray(value) ? value.map(normalize) : value && typeof value === 'object' ? Object.fromEntries(Object.keys(value).sort().map((key) => [key, normalize(value[key])])) : value; const stable = JSON.stringify(normalize(canonical)); const hash = crypto.createHash('sha256').update(stable).digest('hex'); console.log(JSON.stringify({ folioId:'folio-002r', fingerprint:hash, files }, null, 2))
