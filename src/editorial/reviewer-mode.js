import diplomatic from '../data/transcriptions/folio-002r-diplomatic.json'
import findings from '../data/reviews/findings-002r.json'

export function renderReviewerMode(root) { root.innerHTML = `<main class="editorial-tool" aria-labelledby="reviewer-title"><p class="eyebrow">Project GIGAS · reviewer mode</p><h1 id="reviewer-title">Review candidate folio 2r</h1><p>Load a candidate JSON package locally. Canonical records remain unchanged. Candidate acceptance is not automatic verification.</p><p><a href="/editor/folio-002r/compare">Comparison view</a> · <a href="/">Return to reader</a></p><label class="file-button">Load candidate JSON<input id="review-candidate" type="file" accept="application/json"></label><div id="review-candidate-status" role="status" aria-live="polite"></div><section class="workspace-panel"><h2>Canonical status</h2><p>Diplomatic segments: ${diplomatic.segments.length}</p><p>Open blocking findings: ${findings.filter((finding) => finding.severity === 'blocking' && finding.status !== 'resolved').length}</p></section><section class="workspace-panel"><h2>Review findings</h2><ul>${findings.map((finding) => `<li><strong>${finding.severity}</strong> · ${finding.description}</li>`).join('')}</ul></section></main>`; root.querySelector('#review-candidate').addEventListener('change', async (event) => { const file=event.target.files[0]; if (!file) return; try { const candidate=JSON.parse(await file.text()); const segments=candidate.segments || candidate.diplomatic?.segments || []; root.querySelector('#review-candidate-status').textContent=`Loaded candidate ${file.name}: ${segments.length} segment(s). Review findings must be exported separately; canonical data was not changed.` } catch (error) { root.querySelector('#review-candidate-status').textContent=`Candidate load blocked: ${error.message}` } }) }
