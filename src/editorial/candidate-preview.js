export function renderCandidatePreview(root) { root.innerHTML = `<main class="editorial-tool" aria-labelledby="candidate-preview-title"><p class="eyebrow">Project GIGAS · candidate preview</p><h1 id="candidate-preview-title">First-folio candidate preview</h1><div class="workspace-warning" role="alert"><strong>UNVERIFIED AI-GENERATED CANDIDATE</strong><br>NOT PEER REVIEWED · NOT CANONICAL · NOT RELEASE ELIGIBLE</div><p>Load a verified first-folio review-package manifest locally. This preview is isolated from the public canonical reader.</p><label class="file-button">Load package manifest<input id="candidate-package" type="file" accept="application/json"></label><section id="candidate-preview-content" class="workspace-panel" hidden></section></main>`; root.querySelector('#candidate-package').addEventListener('change', async (event) => { const file = event.target.files[0]; if (!file) return; const target = root.querySelector('#candidate-preview-content'); try { const manifest = JSON.parse(await file.text()); if (manifest.canonical !== false) throw new Error('candidate preview requires canonical=false'); target.hidden = false; target.innerHTML = `<h2>Candidate package</h2><p>Source: <code>${manifest.sourceId || 'unknown'}</code></p><p>Status: <strong>UNVERIFIED AI-GENERATED CANDIDATE</strong></p><p>Rights, source image, diplomatic candidate, expansion, literal English, readable English, uncertainties, disagreements, provenance, and blockers must be reviewed from the package records.</p><p>Release eligibility: <strong>blocked</strong></p>` } catch (error) { target.hidden = false; target.textContent = `Candidate preview blocked: ${error.message}` } }) }
