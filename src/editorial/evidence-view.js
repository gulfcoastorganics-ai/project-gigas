import { loadFolio } from '../data/folio-loader.js'
import layout from '../data/layouts/folio-002r.json'
import regions from '../data/regions/folio-002r.json'
import alignments from '../data/alignments/folio-002r.json'

export async function renderEvidenceView(root) { const folio = await loadFolio('folio-002r'); root.innerHTML = `<main class="editorial-tool evidence-view" aria-labelledby="evidence-title"><p class="eyebrow">Project GIGAS · evidence view</p><h1 id="evidence-title">Folio 2r synchronized reading</h1><p>Overlays are optional and currently limited to draft layout guides. No unverified text is presented as a transcription.</p><p><a href="/">Return to reader</a> · <a href="/editor/folio-002r/compare">Open comparison</a> · <a href="/editor/folio-002r/regions">Author regions</a></p><div class="evidence-grid"><section class="evidence-image"><div class="evidence-viewport"><img src="${folio.imageSource.src}" alt="Codex Gigas folio 2r display derivative"><div class="draft-layout-overlay">${layout.regions.map((region) => `<button type="button" class="layout-region" aria-label="${region.label}" style="left:${region.x * 100}%;top:${region.y * 100}%;width:${region.width * 100}%;height:${region.height * 100}%">${region.label}</button>`).join('')}</div></div><small>Draft guides: ${layout.regions.length}; authored text regions: ${regions.length}; alignments: ${alignments.length}</small></section><section class="evidence-text"><h2>Aligned text</h2><p class="classification-label">Diplomatic transcription · placeholder/incomplete</p><p>No canonical text segments are available. This state is intentional pending a human transcription and review.</p><p class="classification-label">Translations · placeholder/incomplete</p><p>No literal or readable translation is displayed.</p></section></div></main>` }
