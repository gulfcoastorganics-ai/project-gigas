# Citation model

`citations.json` contains normalized records. Use `getCitationById()`, `getCitationsForFolio()`, and `formatCitation()` instead of formatting source text in components. Placeholder citations are synthetic fixtures and must be replaced before publication.
