# Editorial status

Open `/editorial-status` for a read-only local workflow dashboard. Percentages describe documentation and workflow completion, not historical accuracy. The dashboard intentionally shows image coverage separately from text and alignment coverage.
