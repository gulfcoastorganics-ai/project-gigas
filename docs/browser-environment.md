# Browser environment

`npm run browser:probe` reports browser binaries, versions, runner packages, and compatible combinations without modifying the system. The current Crostini workspace has no Chromium/Chrome/Firefox executable and no Playwright/Puppeteer package.
