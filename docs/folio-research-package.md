# Folio research package

`npm run export:folio -- --folio=002r` writes JSON and Markdown under `exports/`. The package includes provenance, captures, integrity, transformations, layout, text collections, reviews, findings, change history, audit summary, and release status. Assets are included only when their license allows redistribution; otherwise checksums and references are retained.
