# Candidate export pipeline

Edit IndexedDB, validate, compare against the base fingerprint, export a candidate package, review it, then deliberately place canonical files into the repository. Browser state never overwrites source JSON.
