# Reading order

Text-line drafts use `column`, `lineNumber`, and `readingOrder`. Validation rejects duplicate reading order and reports geometry issues. Candidate generation is never canonical approval.
