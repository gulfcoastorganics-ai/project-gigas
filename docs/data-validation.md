# Data validation and audits

Run `npm run validate:data` to validate IDs, cross-references, states, folios, uncertainty ranges, and tile manifests. Run `npm run audit:sources` to generate `reports/source-audit.md`. Audit percentages measure documentation coverage, not historical accuracy.
