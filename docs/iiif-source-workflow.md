# IIIF source workflow

The synchronized Manuscripta IIIF manifest is parsed into source-page inventory records. Canvas IDs and image-service IDs are retained; folio labels remain null unless supplied by the manifest.
