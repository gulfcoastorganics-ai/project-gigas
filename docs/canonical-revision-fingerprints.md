# Canonical revision fingerprints

Run `npm run fingerprint:folio -- --folio=002r`. The SHA-256 fingerprint covers canonical layout, regions, text layers, translations, alignments, uncertainties, reviews, findings, and change history. It supplements Git history and detects stale browser drafts.
