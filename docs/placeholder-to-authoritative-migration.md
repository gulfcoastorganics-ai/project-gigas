# Placeholder-to-authoritative migration

Keep stable folio IDs and deep links. Add source captures, assets, source packages, structured segments, regions, alignments, uncertainties, and review history incrementally. Do not delete placeholder history; replace display content only after provenance and release gates pass. If ingestion fails, restore the prior record and invalidate the affected cache version.
