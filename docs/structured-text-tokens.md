# Structured text tokens

The data model supports ordinary text plus semantic tokens for abbreviations, suspensions, contractions, rubrication, uncertainty, illegibility, damage, supplied text, insertion, deletion, and line/column breaks. Rendering and accessible descriptions must derive from token meaning, not unexplained glyphs.
