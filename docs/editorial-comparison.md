# Editorial comparison

Open `/editor/folio-002r/compare` for a read-only comparison of image, diplomatic, expanded, literal, and readable layers plus open findings. Local notes are stored separately in IndexedDB; canonical JSON remains unchanged.
