# External review package

`npm run export:review-package -- --folio=002r` creates a timestamped specialist package with permitted image/tiles, provenance, citations, license, attribution, layout guides, blank templates, instructions, checksums, and blocked release state. `verify:review-package` checks its manifest and checksums.
