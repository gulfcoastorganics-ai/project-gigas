# Semantic editorial diffs

`npm run diff:folio -- --folio=002r --candidate=<path>` compares stable records, not JSON key order. Text additions/deletions, citation removal, uncertainty removal, authority removal, and verification changes are dangerous changes.
