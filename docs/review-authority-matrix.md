# Review authority matrix

The matrix in `src/data/review-authority-matrix.json` maps scope to permitted roles, minimum qualification state, independence, external-review requirements, and permitted outcomes. UI actions must not bypass this policy.
