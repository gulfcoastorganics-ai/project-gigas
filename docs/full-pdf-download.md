# Full PDF download

The complete official PDF is never the default. A direct URL must be explicitly configured and storage preflight must pass with a 2 GiB margin. Downloads are intended to stream into `.partial` files and resume by HTTP range when available.
