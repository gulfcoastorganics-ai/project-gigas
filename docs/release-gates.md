# Release gates

`npm run release:check` is intentionally nonzero for the Sprint 4 pilot. A record needs image rights/citation, checksums, transcription and translation provenance where displayed, resolved citations, valid verification, visible uncertainty, separated commentary, and no restricted caching conflict before it can become release-ready.
