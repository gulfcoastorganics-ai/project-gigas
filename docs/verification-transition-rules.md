# Verification transition rules

Transitions are explicit and monotonic where possible. Placeholder cannot jump to verified; unreviewed machine assistance cannot become verified. Validation detects invalid transitions but never upgrades states.
