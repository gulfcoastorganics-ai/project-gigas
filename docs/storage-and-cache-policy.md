# Storage and cache policy

Archival source pages, manifests, hashes, raw provider responses, review packages, and canonical data are retained. Only reproducible derived files and failed partials may be pruned.
