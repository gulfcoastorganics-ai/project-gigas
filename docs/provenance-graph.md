# Provenance graph

`npm run export:provenance-graph -- --folio=002r` writes JSON and Markdown nodes/edges for folio, image, citation, capture, contributor, and blocking finding. It is a local export, not a graph database.
