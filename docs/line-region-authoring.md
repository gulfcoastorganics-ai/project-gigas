# Line-region authoring

The region tool creates draft rectangles only. It supports line IDs, column, line number, reading order, duplicate-down, multi-selection with Shift, undo/redo, keyboard nudging, guide visibility, import/export, and IndexedDB recovery. Canonical line regions remain empty until deliberate export and review.
