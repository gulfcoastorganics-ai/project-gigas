# Textual search

The search module can accept structured segment collections by layer. Placeholder text is excluded from authoritative search eligibility. Search results should identify the matched layer, segment, verification state, uncertainty, and aligned region when those records exist.
