# Testing

Run `npm run test:smoke` for deterministic, dependency-free source checks. Run `npm run test:browser` for real browser automation; it fails explicitly when no browser/runner is installed. A full browser automation runner is not installed in this environment.
