# Canonical acceptance proposals

`npm run propose:canonical -- --folio=002r --candidate=<path>` creates a noncanonical proposal with base fingerprint, candidate data, blockers, and mutation=false. Proposal generation never edits canonical files.
